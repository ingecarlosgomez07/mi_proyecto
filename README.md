# Mi Proyecto en Python

Este proyecto está desarrollado en Python usando un entorno virtual local (`.venv`) y una estructura organizada.

## Requisitos

- Python 3.x

## Configuración inicial

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
